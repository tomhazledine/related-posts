export const defaultConfig = {
    in: "./content",
    temp: "./temp"
};

export const defaultArgs = {
    verbose: false,
    auto: false,
    forceSimilarities: false,
    forceSummaries: false
};
